import 'package:flutter/material.dart';

class Beaches extends StatelessWidget {
  const Beaches({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Beaches')));
  }
}
