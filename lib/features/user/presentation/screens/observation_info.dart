import 'package:flutter/material.dart';

class ObservationInfo extends StatelessWidget {
  const ObservationInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Observation Info')));
  }
}
